
export const TOOLTIP_TEXT = {
  COMPARE: (active: boolean) =>
    !active ? "Add to Comparison" : "Delete from Comparison",
  FAVORITE: (active: boolean) =>
    !active ? "Add to My List" : "Delete from My List",

  MAX_LIMIT_COMPARE: "Only 3 funds could be compared",
};
